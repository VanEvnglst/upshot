import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import PropTypes from 'prop-types';

const ImageHandler = props => {
  const { closeModal, onUploadHandler } = props;
}